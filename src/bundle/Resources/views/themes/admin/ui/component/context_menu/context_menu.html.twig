<ul class="ibexa-popup-menu {{ class|default('') }}">
    {% for item in items %}
        {{ include('@ezdesign/ui/component/context_menu/context_menu_item.html.twig', {
                        'label': item.label,
                        'class': item.class|default(''),
                    })|e('html_attr') }}
    {% endfor %}
</ul>
