<script type="{{ type }}"
        src="{{ src }}"
        {{ async ? 'async="' ~ async ~ '"' : "" }}
        {{ defer ? 'defer="' ~ defer ~ '"' : "" }}
        {{ crossorigin ? 'crossorigin="' ~ crossorigin ~ '"' : "" }}
        {{ integrity ? 'integrity="' ~ integrity ~ '"' : "" }}
></script>